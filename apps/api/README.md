# @news-app/api

NestJS backend service for phased migration from `apps/web` API routes.

## Current Phase
- Phase 1: Bootstrap complete (DDD skeleton, config validation, health/readiness, Swagger).

## Run
```bash
pnpm --filter @news-app/api dev
```

## Build
```bash
pnpm --filter @news-app/api build
```
